spring-injecting-beans
======================

This is a maven project. To run this project download it from source. Then import it in STS. After import goto class Main.java and execute Run->Run in Spring Tool Suite.
